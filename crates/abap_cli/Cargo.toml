[package]
name = "abap_cli"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Debug CLI for ABAP lexer, parser, and symbol indexing (JSON output)"

[[bin]]
name = "abap-cli"
path = "src/main.rs"

[dependencies]
abap_ast = { path = "../abap_ast" }
abap_cache = { path = "../abap_cache" }
abap_lexer = { path = "../abap_lexer" }
abap_parser = { path = "../abap_parser" }
abap_symbols = { path = "../abap_symbols" }
serde_json.workspace = true
